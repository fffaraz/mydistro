#!/bin/sh
# No-op wget stub for offline builds.
#
# gnulib's ./bootstrap calls wget to fetch translation PO files from
# translationproject.org; the build containers run with --network none and
# every package is configured with --disable-nls, so silently succeeding here
# lets bootstrap proceed without touching the network.

exit 0
